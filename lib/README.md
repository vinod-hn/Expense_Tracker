Place mysql-connector-j-<version>.jar (and optional third-party jars) in this folder.
This folder is kept in Git, but individual jar files are ignored via root .gitignore to avoid committing binaries.
